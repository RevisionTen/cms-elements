<div {{ editorAttr(element, edit) }} class="{{ elementClasses(element) }}">
    {% if element.data.formUuid is defined and element.data.formUuid %}
        {{ render(controller('RevisionTen\\CmsElements\\Controller\\FormController::renderCmsForm', {
            formUuid: element.data.formUuid,
            element: element,
            defaultData: []
        })) }}
    {% endif %}
</div>
